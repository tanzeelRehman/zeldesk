class EmployeeListModel {
  late String name, email, role;
  late bool isSelected;
  EmployeeListModel({
    required this.name,
    required this.email,
    required this.role,
    required this.isSelected,
  });
}
